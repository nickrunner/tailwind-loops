# Tailwind Loops Server — Environment Variables
# Copy to .env and adjust as needed. All values shown are defaults.

# --- Overpass API ---
# URL of the Overpass API interpreter endpoint.
# Default: public instance (rate-limited, slow). Use local Docker instance for production.
# OVERPASS_ENDPOINT=https://overpass-api.de/api/interpreter
# OVERPASS_ENDPOINT=http://localhost:8080/api/interpreter      # local Docker

# --- Elevation ---
# Directory containing SRTM HGT tiles for elevation enrichment.
# If unset, elevation enrichment is skipped.
# ELEVATION_TILES_DIR=./data/elevation

# --- Cache ---
# Parent directory for the network cache.
# Default: ~/.tailwind-loops (cache stored in <CACHE_DIR>/network-cache/)
# CACHE_DIR=~/.tailwind-loops

# --- Server ---
# PORT=3000
