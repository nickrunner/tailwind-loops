# Tailwind Loops Server — Environment Variables
# Copy to .env and adjust as needed. All values shown are defaults.

# --- Overpass API ---
# URL of the Overpass API interpreter endpoint.
# Default: public instance (rate-limited, slow). Use local Docker instance for production.
# OVERPASS_ENDPOINT=https://overpass-api.de/api/interpreter
# OVERPASS_ENDPOINT=http://localhost:8080/api/interpreter      # local Docker

# --- Elevation ---
# Directory containing SRTM HGT tiles for elevation enrichment.
# If unset, elevation enrichment is skipped.
# ELEVATION_TILES_DIR=./data/elevation

# --- Cache ---
# Parent directory for the network cache.
# Default: ~/.tailwind-loops (cache stored in <CACHE_DIR>/network-cache/)
# CACHE_DIR=~/.tailwind-loops

# --- Enrichment: Mapillary ---
# Access token for the Mapillary API (free, get at mapillary.com/developer).
# Enables stop sign, traffic signal, and crosswalk detection from street-level imagery.
# MAPILLARY_ACCESS_TOKEN=MLY|...

# --- Enrichment: HeiGIT Surface ---
# Path to HeiGIT surface data (.sqlite lightweight or .gpkg GeoPackage).
# Run: python3 scripts/download-heigit-surface.py   to download + extract lightweight SQLite.
# Source: https://data.humdata.org/dataset/united-states-of-america-road-surface-data
# Enables ML-based paved/unpaved surface classification keyed by OSM way ID.
# HEIGIT_SURFACE_FILE=./data/surface/heigit-surface.sqlite

# --- Server ---
# PORT=3000
